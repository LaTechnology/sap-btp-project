_schema-version: "3.2"
ID: utclfrontend
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: utclfrontend-destination-content
  type: com.sap.application.content
  requires:
  - name: utclfrontend-destination-service
    parameters:
      content-target: true
  - name: utclfrontend-repo-host
    parameters:
      service-key:
        name: utclfrontend-repo-host-key
  - name: utclfrontend-uaa
    parameters:
      service-key:
        name: utclfrontend-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: utclfrontend_html_repo_host
          ServiceInstanceName: utclfrontend-html5-srv
          ServiceKeyName: utclfrontend-repo-host-key
          sap.cloud.service: utclfrontend
        - Authentication: OAuth2UserTokenExchange
          Name: utclfrontend_uaa
          ServiceInstanceName: utclfrontend-xsuaa-srv
          ServiceKeyName: utclfrontend-uaa-key
          sap.cloud.service: utclfrontend
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: utclfrontend-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: utclfrontend-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - utclfrontend.zip
      name: utclfrontend
      target-path: resources/
- name: utclfrontend
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: utclfrontend-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: utclfrontend-destination-service
    service-plan: lite
- name: utclfrontend-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: utclfrontend-xsuaa-srv
    service-plan: application
- name: utclfrontend-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: utclfrontend-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
